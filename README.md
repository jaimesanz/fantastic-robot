This project uses python 3.5

# Installation

* clone repo
* create virtualenv
* enter virtualenv
* `sudo apt-get install phantomjs`
* `pip install -r requirements/base.txt`
* now you can run the dev server using `python fantastic_robot/manage.py runserver`
